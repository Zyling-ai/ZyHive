# ZyHive 产品收口进度

> 启动日期：2026-07-31  
> 当前状态：进行中  
> 当前原则：停止扩张非必要功能，先恢复质量、发布和真实使用闭环

## 目标

1. 恢复主分支全量测试、静态检查、后端构建和前端构建。
2. 明确显示稳定核心与 AI 团队经济实验模块的质量状态，后续再完成结构隔离。
3. 建立可重复的安装、首次配置、对话、工具审批、消息渠道、升级和备份恢复验证。
4. 统一版本号、发布产物、安装端点、README、CHANGELOG 和官网来源。
5. 补齐发布校验和、嵌入 UI 一致性和最低前端自动化测试。
6. 明确当前单机自托管 Beta 的能力边界，不提前承诺多租户和企业级高可用。

## 阶段

### P0：恢复质量基线

- [x] 修复 `pkg/aiteam/genesis_test` 测试夹具与默认工资配置失配
- [x] 执行 `go test ./...`
- [x] 执行 `go vet ./...`
- [x] 执行 `go build ./...`
- [x] 执行前端类型检查与构建
- [x] 修复 macOS `/tmp` 规范化导致的 `safefs` 误判并增加回归测试
- [x] 记录剩余失败、根因和处理结论

### P1：收紧持续集成

- [ ] 将稳定核心与实验模块测试状态分开显示
- [x] 校验嵌入 UI 产物与前端源码同步
- [x] 增加关键后端竞态测试
- [x] 引入最小前端单元测试
- [ ] 增加完整安装、升级、备份恢复烟雾测试

### P2：统一发布链

- [x] 发布前自动执行完整质量门槛
- [x] 使用 `npm ci` 和正式 `npm run build`
- [x] 统一构建平台和产物命名
- [x] 为发布产物生成 SHA-256 校验和
- [x] 安装和升级流程验证校验和
- [x] 安装端点代码改为只读取正式 Release
- [ ] 发布下一稳定版并验证 Release、安装端点、README 和 CHANGELOG 的线上一致性

### P3：产品边界与文档

- [x] 区分稳定能力、实验能力和未发布能力
- [x] 校正稳定版本、支持平台、安装来源和 Node 版本要求
- [x] 明确社区使用、部署服务、企业定制和 AGPL 边界
- [ ] 形成下一稳定版发布或暂缓发布结论

### P4：官网同步

- [ ] 确认并记录线上官网对应的真实部署提交
- [x] 清理过期版本、绝对化能力和未经验证的产品表述
- [x] 安装端点绑定正式 Release，不直接跟随主分支
- [x] 建立官网 Worker 自动化测试和发布检查

## 已完成

- [x] 核对仓库测试文件与现有 CI
- [x] 确认主分支近期 CI 失败
- [x] 复现并定位 `genesis_test` 失败原因
- [x] 识别前端无自动化测试
- [x] 识别发布脚本跳过完整测试、类型检查和校验和
- [x] 识别官网、安装脚本和产品仓库未形成单一发布链
- [x] Go 全量测试、静态检查和构建通过
- [x] 关键包 `-race` 竞态检测通过
- [x] 前端 4 个自动化测试及正式构建通过
- [x] 前端依赖审计由 11 个漏洞降为 0
- [x] 四个平台发布干运行及 SHA-256 复核通过
- [x] 官网 Worker 4 个自动化测试通过
- [x] 修复 `gateway.bind` 配置未生效问题，`localhost`、`lan`、`all` 现在有实际监听差异
- [x] 新配置改为生成随机管理令牌，不再使用公开默认值
- [x] 隔离实例启动、`/healthz`、版本接口和鉴权烟雾测试通过
- [x] 新增 `docs/commercial-boundaries.md`，明确当前只有 AGPL-3.0 和付费专业服务

## P0 安全实施（第一批，2026-07-31）

- [x] 管理 API、下载和媒体在令牌为空时改为拒绝访问，不再 fail-open
- [x] 下载和媒体访问限制在已加载成员的工作区内，阻断任意主机绝对路径和符号链接逃逸
- [x] `send_file` 只允许发送当前成员工作区内文件
- [x] Public Chat 使用独立零工具权限注册表，不再继承 Shell、文件、项目、技能和成员环境变量
- [x] 飞书卡片回调验证签名和五分钟时间窗，并阻断进程内重放
- [x] 飞书渠道界面增加 Encrypt Key 和 Verification Token 配置入口
- [x] 增加鉴权、下载、媒体、公开聊天、飞书回调和文件发送安全回归测试
- [x] Go 全量测试、`go vet`、Go 构建、前端 4 个测试、前端正式构建和嵌入 UI 一致性检查通过

第一批边界说明：

- 下载已从“任意主机路径”收紧为“成员工作区路径”，后续仍需改为 Artifact ID + 一次性短时凭证；
- 飞书重放缓存适用于当前单机部署，未来多实例部署需迁移到共享存储；
- Public Chat 当前默认不暴露任何工具，后续通过独立公开能力 Profile 按白名单逐项开放。

发布记录：

- 版本：`26.7.31v1`
- 范围：已有质量与发布链修复、第一批 P0 安全边界、对应测试和文档
- 发布要求：提交并推送 `main` 后，由 `scripts/release.sh` 重新执行全量门禁并生成四平台正式产物

## P0 安全实施（第二批，已完成）

目标：用登记后的 Artifact ID 和一次性短时凭证替代下载链接中的主机绝对路径与长期管理令牌。

- [x] 建立并发安全、自动过期、一次性消费的 Artifact 下载凭证存储
- [x] `/api/download` 只接受 Artifact ID 和短时凭证
- [x] Web 文件卡片和超大文件下载链接统一使用短时凭证
- [x] 保留成员工作区约束作为第二层防护
- [x] 补充凭证错误、过期、重放、工作区外路径和成功下载测试
- [x] Go 全量测试、`go vet`、Go 构建和关键路径 `-race` 通过
- [x] 提交、推送并发布 `26.7.31v2`

发布边界：

- 下载凭证当前保存在单机进程内，服务重启后自动失效；
- 媒体预览和审批 SSE 仍需移除 URL 中的长期管理令牌；
- 历史文件卡片不会被迁移为永久公开链接，过期后需要重新生成。

## P0 安全实施（第三批，已完成）

目标：移除媒体预览和审批 SSE URL 中的长期管理令牌。

- [x] 媒体预览使用一次性 Artifact 凭证并继续执行工作区、类型双重校验
- [x] `show_image` 补齐工作区路径约束
- [x] 审批 SSE 改为先通过管理 API 领取短时、单次连接凭证
- [x] 删除审批 SSE 对长期管理令牌查询参数的兼容
- [x] 补充媒体重放、旧路径拒绝、审批凭证错误、过期和重放测试
- [x] 前端自动化测试增至 5 个并完成正式构建、嵌入 UI 同步
- [x] Go 全量测试、`go vet`、Go 构建和关键路径 `-race` 通过
- [x] 提交、推送并发布 `26.7.31v3`

第三批边界说明：

- 媒体和下载凭证均保存在单机进程内，服务重启后自动失效；
- 历史消息中的旧媒体路径不再自动拼接管理令牌，需重新生成预览；
- 审批 SSE 每次断线重连都会通过管理 API 获取新的单次连接凭证。

## 当前剩余

- 统一所有入口的 Policy、Approval、Audit 和成本控制；
- 增加公共入口并发、频率、会话和后台运行时限；
- 完成 SSRF、后台进程所有权、Session 串流和路径 ID 全面收口；
- 实际部署一次全新安装、在线升级、失败回滚和备份恢复烟雾测试；
- 确认官网线上部署对应的仓库提交；
- 管理层批准下一稳定版版本号和发布范围；
- 主分支保护仍需在 GitHub 仓库设置中启用。

## GitHub 贡献者显示清理

> 目标：移除历史提交消息中的共同作者标记，使 GitHub 只显示正式提交作者。

- [x] 创建远端仓库完整镜像和可恢复 bundle
- [x] 仅重写 `main` 提交消息，删除所有 `Co-authored-by` 行
- [x] 验证重写前后最终文件树完全一致
- [x] 验证新历史不再包含共同作者标记
- [x] 推送隔离清理分支并核对 GitHub 远端提交
- [x] 完成默认分支安全切换

执行结果：

- 新 `main`：`7d7b204a087cdc722cab954a5b1bf57c5ca76234`
- 原 `main` 已从远端删除；本地备份分支为 `main-before-contributor-cleanup-local-20260731`
- 本机恢复包：`/tmp/ZyHive-before-contributors-clean-20260731.bundle`
- 重写前后提交数均为 570，最终 tree 均为 `5589263ecf008ee2a0c0989aba87521477ddafd2`
- GitHub Contributors API 当前只返回 `Zyling-ai`

### 贡献者身份回归修复（2026-07-31）

问题：`26.7.31v1` 至 `v3` 的提交误用了本机 `Yiliu` 身份，并被自动加入 `Cursor` 共同作者，导致 GitHub 页面重新显示其他贡献者。

- [x] 确认错误来源是最近三个提交的 Author、Committer 和 `Co-authored-by`
- [x] 将全部 574 个提交的 Author、Committer 规范为 `Zyling-ai <github@zyling.ai>`
- [x] 移除全历史 `Co-authored-by` 并同步修正 `26.7.31v1`、`v2`、`v3` 标签
- [x] 增加项目级 Cursor 永久规则，禁止其他身份和共同作者
- [x] 增加 CI 全历史提交身份门禁
- [x] 增加发布前提交身份门禁
- [x] 推送新历史并确认 Contributors 只保留 `Zyling-ai`
- [x] 发布身份门禁版本 `26.7.31v4`

防回归结果：

- 规范化前后提交数均为 574，最终文件树均为 `d52053ec2b79daf5a0abf1efe8b224042f60df46`
- 本机恢复包：`/tmp/ZyHive-before-identity-normalization-20260731.bundle`
- 本地恢复分支：`before-identity-normalization-local-20260731`
- `scripts/verify-commit-identity.sh` 会检查全部可达提交，身份或共同作者不合规时阻断 CI 和 Release

## 当前验收标准

- [x] 2026-07-31 已连续三个主分支提交通过 CI；
- 干净环境可完成安装、首次配置、升级、备份和恢复；
- 发布产物可校验，嵌入 UI 与前端源码一致；
- 官网、安装端点、README、CHANGELOG 和 Release 版本一致；
- 稳定版不包含未说明的实验功能；
- 所有测试、文档和发布结论有实际命令或文件证据。

## Release 安装更新全流程门禁（已完成，2026-07-31）

目标：每次正式发布都必须在隔离目录中验证产物校验、全新安装、服务启动、鉴权、基础 API、旧版升级、新版确认和备份保留；任一步失败均不得宣布发布成功。

- [x] 盘点安装脚本、更新链路、基础 API 和现有烟雾测试
- [x] 建立可本地重复执行的 Release 全流程测试模块
- [x] 用本地 Release 仓库模拟全新安装与旧版升级，避免测试污染用户环境
- [x] 将全流程测试接入 `scripts/release.sh` 的发布前门禁
- [x] 增加 GitHub Release 发布后的线上安装校验工作流
- [x] 补充 Makefile 入口和维护文档
- [x] 执行本地失败/成功路径、全量测试与 CI 验证
- [x] 提交、推送并发布首个带全流程门禁的版本 `26.7.31v5`

交付结果：

- 测试模块：`scripts/test/release-e2e/`
- 本地入口：`make release-e2e RELEASE_VERSION=<版本>`
- 正式发布：`scripts/release.sh` 在创建 Release 前执行隔离全流程测试，创建后执行正式资产在线测试
- 自动复测：`.github/workflows/release-e2e.yml` 在 Linux、macOS 上验证正式安装和上一个版本到当前版本的更新
- 失败策略：发布前直接阻断；发布后自动把失败版本转为草稿
- 2026-07-31 本地证据：错误版本会返回非零；`26.7.31v5` 发布干运行、Go 全量测试、前端 5 项测试、四平台构建和本地安装更新全流程通过

## Release E2E 严格性补强（已完成，2026-07-31）

- [x] 增加禁止外部回退模式，确保本地与镜像测试不会被 GitHub 回退掩盖
- [x] 将缺失校验和改为阻断安装
- [x] 修复 `--no-service`、系统配置写入和配置文件权限
- [x] 增加成员 CRUD、工作区文件和跨更新持久化验证
- [x] 增加错误校验和不替换旧二进制回归
- [x] 线上门禁直接验证 `install.zyling.ai/latest`、安装脚本和下载代理
- [x] 完成全量验证并发布严格门禁版本 `26.7.31v6`

## P0 公共入口与出站访问收口（进行中，2026-07-31）

目标：公共聊天等无需管理令牌的入口必须具备明确的频率、并发、会话和执行时间上限；所有可控出站 URL 必须统一阻断本机、内网、元数据地址和 DNS 重绑定。

- [x] 盘点公共入口、出站请求和后台执行边界
- [x] 增加公共入口按来源限流、全局任务/SSE 并发和会话上限
- [x] 增加公共任务硬超时和消息大小上限
- [x] 建立统一 SSRF URL/DNS 校验与安全 HTTP Client
- [x] 首批接入 `web_fetch` 和浏览器初始导航
- [x] 补充限流、并发、会话、内网地址、云元数据和 DNS 重绑定回归测试
- [x] Go 全量测试、`go vet`、前端 5 项测试、依赖审计和 Release E2E 干运行通过
- [x] `26.7.31v7` 本机正式在线验证和 Linux 远端验证通过
- [x] 定位 macOS 节点因 GitHub latest 跨地区传播延迟触发自动撤回
- [x] 将矩阵撤回改为两平台结束后统一判定，并把 latest 等待扩展到 5 分钟
- [x] 提交、推送并发布修复门禁竞态的 `26.7.31v8`
- [x] 浏览器全部 HTTP、HTTPS、WebSocket 和动态页面请求随 `26.7.31v10` 收口

## P0 会话 Worker 隔离与终止语义（进行中，2026-07-31）

目标：不同成员、不同公开渠道不得因相同 Session ID 共享 Worker；每个会话同一时间只允许一个任务，并且成功、失败或超时都必须恰好发送一次终止事件。

- [x] WorkerPool 使用所有者 + Session ID 复合键
- [x] Public Chat 使用成员 + Web 渠道作为 Worker 所有者
- [x] 拒绝同会话并发或排队请求，避免代际缓冲区串流
- [x] 将 `StartGen` 收口到成功入队的唯一位置
- [x] Worker 成功、失败和 panic 路径统一补齐且去重终止事件
- [x] 公共入口增加全局活跃会话上限
- [x] 补充所有权、并发、终止事件、panic 和会话容量回归
- [x] Go 全量测试、关键包竞态、`go vet`、前端 5 项测试、依赖审计和 Release E2E 干运行通过
- [x] 提交、推送并发布 `26.7.31v9`

## P0 浏览器完整网络隔离（已完成，2026-07-31）

目标：不仅校验 `browser_navigate` 的初始 URL，还要让 Chromium 的重定向、页面脚本、图片、iframe、XHR、WebSocket、点击和 `browser_eval` 出站请求全部经过同一 SSRF 安全拨号边界。

- [x] 扩展 `pkg/netguard` 安全 Transport 与拨号接口
- [x] 建立仅监听回环随机端口的 HTTP/CONNECT 安全代理
- [x] 强制 Chromium 使用安全代理并取消 localhost 默认绕过
- [x] 禁用 QUIC，避免 UDP 直连绕过代理
- [x] 浏览器关闭时同步关闭代理
- [x] 增加 HTTP、CONNECT、WebSocket、内网地址、云元数据和代理生命周期回归
- [x] 使用真实 Chromium 验证页面子资源无法访问本机
- [x] Go 全量测试、静态检查、前端测试、依赖审计和 Release E2E 干运行通过
- [x] 提交、推送并发布 `26.7.31v10`
- [x] 主分支 CI、Linux/macOS Release E2E、正式安装和 `v9 → v10` 更新验证通过
- [x] 国内安装镜像已切换到 `26.7.31v10`
- [x] GitHub Contributors 仍仅显示 `Zyling-ai`

## P0 统一 Policy / Approval / Audit（已完成，2026-07-31）

目标：Web、Telegram、飞书、Cron、Heartbeat 和 Subagent 使用同一套工具权限合并、人工审批和工具审计规则；任何配置错误或审批服务缺失均不得静默放行。

- [x] 梳理所有 Runner 与工具注册入口的策略、审批和审计覆盖差异
- [x] 统一全局与成员 Policy 独立约束、未知配置和未知分组的 fail-closed 语义
- [x] Policy 在全部动态工具注册完成后执行，且后注册工具仍受既有策略限制
- [x] Approval Broker 在后台任务启动前注入渠道、Cron、Heartbeat 和 Subagent
- [x] 审批服务不可用时拒绝 `ask` 工具，禁止静默执行
- [x] Pool 创建的全部 Runner 均写入成员级工具审计，审计写入失败显式记录
- [x] 修复审批决定/取消唯一终态、订阅取消并发竞态和客户端伪造决策人
- [x] 审批 SSE 首帧与心跳携带权威待办快照，前端重连和决策后主动重新同步
- [x] Go 全量测试与竞态检测、静态检查、前端测试、类型检查、构建和依赖审计通过
- [x] Release E2E 干运行通过全新安装、基础 API、持久化和 `v10 → v11` 更新
- [x] 提交、推送并发布 `26.7.31v11`
- [x] 主分支 CI、Linux/macOS Release E2E、正式安装和 `v10 → v11` 更新验证通过
- [x] 国内安装镜像已切换到 `26.7.31v11`
- [x] GitHub Contributors 仍仅显示 `Zyling-ai`

## P0 后台进程所有权与回收（进行中，2026-08-01）

目标：`bash(background=true)` 与 ACP 子进程必须绑定成员和会话，禁止跨成员/跨会话查看、写入或终止，并具备进程组回收、数量限制、输出上限、最长运行时间和过期清理。

- [x] 将 Bash 与 ACP 后台任务收口到统一进程存储
- [x] 进程身份统一为 `{agentID, sessionID, processID}`，所有操作校验所有者
- [x] `process list` 仅返回当前成员、当前会话任务
- [x] 增加每个所有者与全局并发上限
- [x] 增加标准输入管道、输出上限、最长运行时间和完成后过期清理
- [x] 取消或超时时终止完整进程组，禁止子进程残留
- [x] ACP 工作目录限制在成员工作区，环境变量按最小权限注入
- [x] 补充跨所有者隔离、并发、输出、超时、进程树和 ACP 回归测试
- [ ] 完成全量验证并发布 `26.8.1v1`

## 全面优化计划制定（2026-07-31）

> 目标：对照 2026 年优秀 Agent、自动化与自托管项目，形成 ZyHive 下一阶段可执行的产品、架构、体验、质量和生态优化路线。

- [x] 盘点现有产品能力、代码架构与模块边界
- [x] 审查测试、CI、发布、安全与可观测性现状
- [x] 对标当前优秀开源 Agent 和自动化项目
- [x] 明确应该保留、重构、隔离和停止的能力
- [x] 制定 30 天、75 天、6 个月和 12 个月路线
- [x] 形成指标、质量门槛、版本策略和执行优先级
- [x] 输出渐进式披露计划文档和可视化路线图

交付结果：

- 完整文档：`docs/optimization-roadmap-2026-2027.md`
- 可视化路线：Cursor Canvas `zyhive-optimization-roadmap-2026.canvas.tsx`
- 产品方向：个人与小团队的自托管 AI 团队
- 总体结论：暂停功能扩张，先关闭安全、运行、产品和发布 Stop-Ship 问题，再建设可靠 Run Engine
- 发布前远端基线：连续 6 次 `main` CI 失败，且没有分支保护
- 当前本地验证：Go 全量测试、`go vet`、前端 4 个测试、依赖审计和 `git diff --check` 通过
- 当前质量缺口：Go 总语句覆盖率约 22.4%，前端仅 1 个测试文件；公开入口、路径、进程、权限、会话和秘密边界仍有发布阻断项
